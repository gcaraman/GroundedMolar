# Build specification source

The authoritative initial specification was supplied in the originating Codex task on 2026-08-23. Its invariants are distilled into `AGENTS.md`, milestones into `ROADMAP.md`, and verified facts into `DECISIONS.md`.

Every marker must trace from current save → selected spawn GUID → persistent actor state → that spawn's transform → validated projection → calibrated viewport.
