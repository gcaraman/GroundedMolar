# Microsoft Store visual assets

- `MolarMap-StoreLogo-300x300.png` is marketplace-only preview artwork. The installed app and MSI continue to use the original single-molar `MolarMap.ico` with no map background.
- At least one real 1366 × 768-or-larger app screenshot must be added before submission. Capture a clean MolarMap window using a non-private test save, and check that no save path, account name, private GUID, or unrelated desktop content is visible.
- Four or more real screenshots are recommended: empty-state save selection, validated map overview, a marker popup with community hints disabled, and Preferences.

Do not use generated or retouched screenshots to imply app behavior. Use only MolarMap/Grounded imagery that the uploader is entitled to publish.
